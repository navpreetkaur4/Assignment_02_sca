name: Bandit Security Scan

on:
  push:
  pull_request:

jobs:
  bandit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run Bandit
        uses: pyupio/bandit-action@v1
        with:
          args: "-r ."
